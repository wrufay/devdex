{
  "name": "study-terminal",
  "version": "1.0.0",
  "description": "Gamified study terminal with flashcards and spaced repetition",
  "main": "src/index.js",
  "bin": {
    "study": "./src/index.js"
  },
  "scripts": {
    "build": "esbuild src/index.js --bundle --platform=node --outfile=dist/app.mjs --format=esm --jsx=automatic --loader:.js=jsx --define:process.env.NODE_ENV=\\\"production\\\" --banner:js=\"import { createRequire } from 'module'; const require = createRequire(import.meta.url);\"",
    "start": "npm run build && NODE_ENV=production node dist/app.mjs",
    "dev": "npm run build && NODE_ENV=production node dist/app.mjs",
    "test": "node --test src/**/*.test.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "@inkjs/ui": "^2.0.0",
    "@supabase/supabase-js": "^2.106.2",
    "chalk": "^5.6.2",
    "dotenv": "^17.4.2",
    "ink": "^6.6.0",
    "ink-select-input": "^6.2.0",
    "ink-spinner": "^5.0.0",
    "ink-text-input": "^6.0.0",
    "react": "^19.2.4",
    "react-devtools-core": "^6.1.5"
  },
  "devDependencies": {
    "esbuild": "^0.27.2"
  }
}
